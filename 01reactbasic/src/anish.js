//the file can be .jsx also, no problem in this, but it creates problem in vite application. Vite application accepts onlr .jsx files
//function names should be starting with caps. THIS IS MANDATORY
function Fun(){ //function name should start with caps, default practices of react, which not followed creates errors

    return(
        <h2>Our own file is ready!</h2>
    )
}

export default Fun

